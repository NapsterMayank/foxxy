.next/
coverage/
node_modules/
playwright-report/
test-results/
.env*
!.env.example

# Lighthouse CI output — reports, not sources.
.lighthouseci/

# The production `.next` extracted from the container build (open item 33 —
# `next build` cannot complete on a Windows host, so the real one is produced
# on Linux and copied out for the bundle-budget gate).
.next-from-image/
