# knowledge — decisions

**The decision log for this module is `docs/03-DECISION-LOG.md` at the repo
root, and it is the only one.** This file is a pointer, deliberately empty of
content: two logs describing the same decision drift, and the drift always
outlives the change that caused it.

Entries touching `modules/knowledge`:

| Entry | Subject |
|---|---|
| D-060 | A path is re-ordered after hydration — `IN (...)` returns any order |
| D-105 | `concept_graph.concept_code` does not join to `chapter_concepts` |
| D-129 | A feature built on a thin graph looks like it works |
| **D-193** | `orderable` is measured on the graph the feature walks; `plannableChapters` added |
